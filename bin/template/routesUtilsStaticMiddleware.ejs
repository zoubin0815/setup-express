/**
 * 
 * @param {Object} req 响应的请求
 * @param {Object} res 获得的请求
 * @param {Funtion} next 执行向下传递
 */
module.exports = (req , res , next) => {
    if(req.path.startsWith("/api")){
        // 说明你请求的是api接口
    }else{
        // 说明你想要的是静态资源
        if(true){
            res.send("静态资源")
        }else{
            next();
        }
    }
}